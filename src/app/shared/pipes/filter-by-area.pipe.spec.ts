import { FilterByAreaPipe } from './filter-by-area.pipe';

describe('FilterByAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
